require 'rails_helper'

	#describe fii do
		#context 'given an empty string' do
		#	it "returns blank page" do
		#		expect(response).to_not contains("<tr>")
		#	end
		#end


		#it "has a 200 status code" do
	   #   	expect(response.status).to eq(200)
	   # end

#		context 'given an string' do
#			it "returns table content" do
#				get :fii 
#				#expect (NoticiasController.fii).to_not render_template(:fii)
#				expect(response).to_not eq("<tr>")
#			end
#		end

		#it 'calls service to retrieve news'
		#it 'receive an xml'
		#it 'send to the view the content of the search'
	#end